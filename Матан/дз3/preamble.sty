\usepackage[utf8]{inputenc}
%%\usepackage[cp1251]{inputenc}
%\usepackage[T2A]{fontenc}
\usepackage[english,russian]{babel}

\usepackage{amsmath, amssymb, amsfonts, amsthm}
\usepackage{graphicx,url}
\usepackage[dvipsnames]{xcolor}
\let\tg\void
\let\ctg\void
\let\cosec\void
\let\arctg\void
\let\arcctg\void
\let\cth\void
\let\ch\void
\let\sh\void
%%\usepackage{russcorr}
%%\usepackage{fullpage}

\makeatletter
\renewcommand*\env@matrix[1][*\c@MaxMatrixCols c]{%
	\hskip -\arraycolsep
	\let\@ifnextchar\new@ifnextchar
	\array{#1}}
\makeatother


\headheight=0mm \headsep=0mm
\textwidth=180mm \textheight=256mm \oddsidemargin=-10mm
\topmargin=-5mm
\parindent=0pt
\parskip=1.5mm
\voffset=-16mm
\def\bfdefault{b}	
\def\thefootnote{\arabic{footnote})}


\newcounter{problem}
\setcounter{problem}{0}
\newcounter{subproblem}
\setcounter{subproblem}{0}
\newcommand{\p}{\refstepcounter{problem}\setcounter{subproblem}{0}
{\bfseries\theproblem.}~}
\renewcommand{\thesubproblem}{\asbuk{subproblem}}
\renewcommand{\sp}{\refstepcounter{subproblem}{\bfseries\thesubproblem)}~}
\newcommand{\dop}{\refstepcounter{problem}\setcounter{subproblem}{0}
{\bfseries\theproblem*.}~}

\let\ds\displaystyle
\def\theenumi{\ralph{enumi}}

\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Q}{\mathbb{Q}}
\def\R{\mathbb{R}}
\newcommand{\sgn}{\mathop{\mathrm{sgn}}}  % sign
\let\ds\displaystyle
\let\leq\leqslant
\let\geq\geqslant
\let\le\leqslant
\let\ge\geqslant
\let\la\langle
\let\ra\rangle
\let\emptyset\varnothing
\def\RR{\mathbb{R}}
\def\QQ{\mathbb{Q}}
\def\NN{\mathbb{N}}
\def\HH{\mathbb{H}}
\def\ZZ{\mathbb{Z}}
\def\CC{\mathbb{C}}
\def\FF{\mathbb{F}}
\def\id{\mathop{\mathrm{id}}\nolimits}
\let\epsilon\varepsilon
\let\eps\varepsilon
\let\al\alpha
\let\sg\sigma
\let\ga\gamma
\let\ph\varphi
\let\om\omega
\let\ld\lambda
\let\vk\varkappa
\let\Om\Omega
\let\Ld\Lambda
\let\es\varnothing
\def\Ker{\mathop{\mathrm{Ker}}}
\def\Re{\mathop{\mathrm{Re}}}
\def\Im{\mathop{\mathrm{Im}}}
\def\mex{\mathop{\mathrm{mex}}}
\def\op#1{\mathop{\mathrm{#1}}\nolimits}
\def\gcd{\op{\text{НОД}}}
\def\lcm{\op{\text{НОК}}}

\def\iff{\ \Leftrightarrow\ }
\def\sym{\mathbin{\triangle}}
\let\sm\setminus
\def\lif{\mathbin{\to}}
\def\0{\text{\sf Л}}
\def\1{\text{\sf И}}
\def\Dom{\mathop{\mathrm{Dom}}}
\def\Range{\mathop{\mathrm{Range}}}
\def\Val{\mathop{\mathrm{Val}}}
\def\bydef{\mathbin{\stackrel{\text{\tiny def}}{=}}}
\let\wbeg\triangleleft
\let\wend\triangleright
\def\rewrite#1#2{\xrightarrow[#2]{#1}}

\def\mpfile#1#2{\includegraphics{#1-#2.mps}}

%% \pagestyle{empty}
\usepackage{fancyhdr}

\pagestyle{fancy}
\renewcommand{\headrulewidth}{0pt}
\lfoot{\footnotesize \textsl {ФКН ВШЭ, 2023/24 уч.\,г.}}
\cfoot{\empty}
